<!-- Main content -->
<div class="content-wrapper">
  <section class="content-header">
    <div class="container-fluid">
      <div class="row mb-2">
        <div class="col-sm-6">
          <h1>Users List</h1>
        </div>
        <div class="col-sm-6">
          <ol class="breadcrumb float-sm-right">
            <li class="breadcrumb-item"><a href="#">Home</a></li>
            <li class="breadcrumb-item active">Users</li>
          </ol>
        </div>
      </div>
    </div>
  </section>
  <section class="content">
    <div class="container-fluid">
      <div class="row">
        <div class="col-12">
          <div class="card">
            <div class="card-header">
              <h3 class="card-title"></h3>
              <div style="float: right;"><a href="/admin/users/new">Add New</a></div>
            </div>
            <div class="card-body">
            <%# <%= debug(@users) if Rails.env.development? %>
              <table id="article_data" class="table table-bordered table-hover data_table">
                <thead>
                  <tr>
                    <th>#ID</th>
                    <th>User Name</th>
                    <th>Email</th>
                    <th>Create Date</th>
                    <th>Action</th>
                  </tr>
                </thead>
                <tbody>
                  <% @users.each do |data| %>
                    <tr>
                      <td><%= data.id %></td>
                      <td><%= data.user_name %></td>
                      <td><%= data.email %></td>
                      <td><%= data.created_at %></td>
                      <td width="15%">
                        <i class="fas fa-edit"></i> <%= link_to 'Edit', {:action => 'edit', :id => data.id} %>
                        <i class="fas fa-exchange-alt"></i> <a style="cursor:pointer;" href="/admin/users/<%= data.id %>/change_password" title="password Change">Change</a>
                        <i class="fas fa-trash-alt"></i> <%= link_to 'Delete',url_for(action: :destroy,id: data.id),method: :delete, data: {confirm: "Are you sure you want to delete this item?"} %>
                      </td>
                    </tr>
                  <%end%>
                </tbody>
              </table>  
            </div>
          </div>
        </div>
      </div>
    </div>  
  </section>
</div>        